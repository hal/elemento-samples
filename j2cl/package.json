{
  "name": "elemento-samples-j2cl",
  "description": "J2CL Elemento Sample",
  "private": true,
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "parcelIgnore": [
    "todo.j2cl.js"
  ],
  "source": "src/web/index.html",
  "scripts": {
    "watch": "parcel serve --dist-dir target/todo --open",
    "build": "parcel build --public-url \"./\" --dist-dir target/todo"
  },
  "devDependencies": {
    "@parcel/reporter-cli": "^2.10.2",
    "parcel": "^2.10.2",
    "parcel-resolver-ignore": "^2.1.5"
  },
  "dependencies": {
    "todomvc-app-css": "^2.4.3",
    "todomvc-common": "^1.0.5"
  }
}
