{
  "name": "elemento-samples-cryskni",
  "description": "Crysknife Elemento Sample",
  "private": true,
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "parcelIgnore": [
    "todo.j2cl.js"
  ],
  "source": "src/web/index.html",
  "scripts": {
    "watch": "parcel serve --dist-dir target/todo --open",
    "dev": "parcel build --dist-dir target/todo --no-optimize",
    "prod": "parcel build --dist-dir target/todo"
  },
  "devDependencies": {
    "@parcel/reporter-cli": "^2.4.0",
    "@parcel/transformer-less": "^2.4.0",
    "parcel": "^2.4.0",
    "parcel-resolver-ignore": "^2.0.0"
  },
  "dependencies": {
    "todomvc-app-css": "^2.4.2"
  }
}
