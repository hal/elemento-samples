{
  "name": "elemento-samples-crysknife",
  "description": "Crysknife Elemento Sample",
  "private": true,
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "parcelIgnore": [
    "todo.j2cl.js"
  ],
  "source": "src/web/index.html",
  "scripts": {
    "watch": "parcel serve --public-url \"./\" --dist-dir target/todo --open",
    "build": "parcel build --public-url \"./\" --dist-dir target/todo"
  },
  "devDependencies": {
    "@parcel/reporter-cli": "^2.5.0",
    "parcel": "^2.6.0",
    "parcel-resolver-ignore": "^2.1.1"
  },
  "dependencies": {
    "todomvc-app-css": "^2.4.2",
    "todomvc-common": "^1.0.5"
  }
}
